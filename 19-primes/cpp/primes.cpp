#include <vector>

std::vector createSieve(int limit) {
  std::vector primes = { true; limit };

  return primes;
}

int main() {
  int target = 32;

  std::vector sieve = createSieve(target);

  return 0;
}